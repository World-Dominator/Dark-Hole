[project]
name = "darkhole"
version = "0.0.0"
description = "Darkhole research prototype"
requires-python = ">=3.10"
dependencies = ["cryptography>=41"]

[tool.pytest.ini_options]
testpaths = ["tests"]
